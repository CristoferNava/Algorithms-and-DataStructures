# Given two strings, check to see if they are anagrams. 
# An anagram is when the two strings can be written using the exact same 
# letters (so you can just rearrange the letters to get a different phrase or 
# word).
# Note: Ignore spaces and capitalization. So "d go" is an anagram of "God" and 
# "dog" and "o d g"

def is_anagram(str1: str, str2: str) -> bool:
    pass

# Test cases
print(is_anagram('dog','god')) # True
print(is_anagram('clint eastwood','old west action')) # True
print(is_anagram('aa', 'bb')) # False
