#include <iostream>

using namespace std;

int main() {
    // short code is ideal because programs should be written as fast as possible
    // using the command typedef it is possible to give a shorter name to a datatype
    typedef long long ll;
    ll a = 1234567890;
    cout << a << "\n";
    
    return 0;
}