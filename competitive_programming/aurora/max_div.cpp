#include <iostream>

using namespace std;

int minC(int, int);
int maxC(int);

int main() {

}

int minC(int n1, int n2) {
    // 2 3, mcm=6
    // 2 4, mcm=4
    // 4, 6 mcm=12
    // mcm(2, 3)=6
    // if n1 y n2 both primes then n1 * n2
    int myMax = max(n1, n2);
    myMax % n1 == 0 && myMax % n2 == 0 return myMax 

    // first case, both of them are primes O(n log(n))
    // second case, myMax is the mcm O(1)
    // third case: look up for another candidate: O(n1 * n2)
    // --4--6-12-----24---------------

    // estudiar mcm, mcd y qué tienen en común
    // estudiar código para los primos
}

# AI. ML, DL, DS
# R, Python
# Simple de aplicar, simple de leer
# qué tan bueno es, realmente se usa?

# C++
# SO, servidores, librerías

C++      vs Python
Compilado


int main() {
    cout << "hola" << endl;
}

compilador específico para tu sistema operativo
laptops 
ing software

Java
-- Robusto
-- Escalable 
-- POO (Abstracción, Herencia, Polimorfismo, Encapsulación)
-- Multihilo, concurrente

¿Windows, Mac, Teléfono?

[JVM] Portabilidad

